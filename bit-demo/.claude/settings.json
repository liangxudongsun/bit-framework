{
  "permissions": {
    "allow": [
      "Bash(npx eslint *)",
      "Bash(npm run lint *)",
      "Bash(npm test *)",
      "Glob(*)",
      "Grep(*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "filepath=$(echo \"$TOOL_INPUT\" | jq -r '.file_path // .filePath // empty') && if [ -n \"$filepath\" ] && echo \"$filepath\" | grep -q '\\.ts$' && echo \"$filepath\" | grep -q '/assets/script/'; then cd \"$PROJECT_DIR\" && npx eslint --fix \"$filepath\" 2>/dev/null || true; fi"
          }
        ]
      }
    ]
  }
}
